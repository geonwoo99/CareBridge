// lib/site.config.ts
// ─────────────────────────────────────────────────────────────
// 이 파일은 사이트의 "단일 출처(single source of truth)"입니다.
// 브랜드명, 연락처, 면책 문구 등을 오직 여기서만 정의하고,
// 사이트의 모든 곳은 이 값을 가져다 씁니다.
//
// ▶ 나중에 병원에 소속되거나 브랜드가 바뀌면, 이 파일만 고치면
//   사이트 전체(헤더/푸터/CTA/메타데이터)가 한 번에 바뀝니다.
//   다른 파일은 건드릴 필요가 없습니다.
// ─────────────────────────────────────────────────────────────

export const siteConfig = {
  // 사이트에 표시되는 운영자 이름/브랜드
  brand: "CareBridge",
  tagline: "보호자를 위한 반려동물 건강 가이드",

  // 사이트 기본 URL (배포 후 실제 도메인으로 교체). QR 코드 생성에 사용됩니다.
  baseUrl: "https://example.com",

  // ── 연락처 ──────────────────────────────────────────────
  // 아직 병원 소속이 아니므로 비워둡니다(null).
  // null이면 관련 버튼/블록이 화면에 "아예 표시되지 않습니다".
  // 나중에 값이 생기면 채우기만 하면 자동으로 나타납니다.
  contact: {
    phone: null as string | null,        // 예: "02-1234-5678"
    kakaoUrl: null as string | null,     // 예: "http://pf.kakao.com/_xxxx/chat"
    hours: null as string | null,        // 예: "월–토 09:00–19:00 · 일/공휴일 휴진"
  },

  // ── 면책 문구 ───────────────────────────────────────────
  // 모든 교육자료 하단에 자동으로 삽입됩니다(글마다 수동으로 쓰지 않음).
  disclaimer:
    "본 자료는 보호자 교육용이며, 수의사의 진단·진료를 대체하지 않습니다.",

  // ── 상단 출처 알림(SourceNotice) 공통 안내 문구 ─────────
  // 모든 가이드 글 본문 위에 자동 삽입됩니다.
  disclaimerNotes: [
    "본 콘텐츠는 논문·가이드라인 등 신뢰할 수 있는 자료와 수의사의 임상 경험을 바탕으로 작성되었습니다.",
    "다만 일반적인 경우에 대한 안내이며 아이마다 상태와 상황이 다를 수 있으니, 우리 아이에게 맞는 정확한 진단과 처방은 반드시 담당 수의사와 상담해주세요.",
  ] as readonly string[],

  // ── 점수 저장 안내 문구 ────────────────────────────────
  // 평가표에서 식별코드 입력 시 보여줄 안내(보안 기능이 아님을 알림).
  scoreStorageNotice:
    "입력하신 이름과 숫자는 기록을 간단히 구분하기 위한 용도이며, 비밀번호가 아닙니다.",
} as const;

// 연락처가 하나라도 있는지 확인하는 헬퍼.
// 컴포넌트에서 이 값으로 CTA 블록 표시 여부를 결정합니다.
export const hasContact =
  !!siteConfig.contact.phone || !!siteConfig.contact.kakaoUrl;
